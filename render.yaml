services:
  - type: web
    name: estudio-vivo
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: npm start
    healthCheckPath: /
    autoDeployTrigger: commit
